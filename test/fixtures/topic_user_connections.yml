# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This model initially had no columns defined.  If you add columns to the
# model remove the '{}' from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#

<% users(:william).topics << topics(:full_stack)

users(:william) << topics(:web)

users(:william) << topics(:programming)

users(:william) << topics(:iOS)

users(:william) << topics(:rails)

users(:william) << topics(:java)

users(:william) << topics(:html)

users(:william) << topics(:mobile)

users(:william) << topics(:javascript)

users(:william) << topics(:jquery)

users(:veni) << topics(:full_stack)

users(:veni) << topics(:web)

users(:veni) << topics(:programming)

users(:veni) << topics(:python)

users(:veni) << topics(:management)

users(:veni) << topics(:android)

users(:veni) << topics(:html)

users(:veni) << topics(:mobile)

users(:veni) << topics(:version_control)

users(:veni) << topics(:subversion)

users(:veni) << topics(:git)

users(:veni) << topics(:communication)

users(:brett) << topics(:full_stack)

users(:brett) << topics(:web)

users(:brett) << topics(:programming)

users(:brett) << topics(:iOS)

users(:brett) << topics(:c_plus_plus)

users(:brett) << topics(:java)

users(:brett) << topics(:html)

users(:brett) << topics(:mobile)

users(:brett) << topics(:algorithms)

users(:brett) << topics(:presentation)

users(:brett) << topics(:management)

users(:john) << topics(:full_stack)

users(:john) << topics(:web)

users(:john) << topics(:programming)

users(:john) << topics(:graphic_design)

users(:john) << topics(:rails)

users(:john) << topics(:java)

users(:john) << topics(:html)

users(:john) << topics(:ruby)

users(:john) << topics(:javascript)

users(:john) << topics(:jquery)

users(:stephen) << topics(:full_stack)

users(:stephen) << topics(:web)

users(:stephen) << topics(:programming)

users(:stephen) << topics(:management)

users(:stephen) << topics(:presentation)

users(:stephen) << topics(:java)

users(:stephen) << topics(:html)

users(:stephen) << topics(:version_control)

users(:stephen) << topics(:javascript)

users(:stephen) << topics(:jquery)

users(:stephen) << topics(:data_visualization)

users(:stephen) << topics(:git)

users(:chris) << topics(:management)

users(:chris) << topics(:communication)

users(:chris) << topics(:sales)

users(:chris) << topics(:presentation)

users(:chris) << topics(:statistics)

users(:mike) << topics(:full_stack)

users(:mike) << topics(:web)

users(:mike) << topics(:programming)

users(:mike) << topics(:iOS)

users(:mike) << topics(:rails)

users(:mike) << topics(:java)

users(:mike) << topics(:html)

users(:mike) << topics(:ruby)

users(:mike) << topics(:javascript)

users(:mike) << topics(:jquery)

users(:mike) << topics(:algorithms)

users(:mike) << topics(:python)

users(:mike) << topics(:fortran)

users(:mike) << topics(:java)

users(:mike) << topics(:php)

users(:mike) << topics(:android)

users(:mike) << topics(:c)

users(:mike) << topics(:c_plus_plus)

users(:mike) << topics(:objective_c)

users(:mike) << topics(:git)

users(:mike) << topics(:subversion)

users(:mike) << topics(:version_control)

users(:mike) << topics(:mobile)

users(:marc) << topics(:full_stack)

users(:marc) << topics(:web)

users(:marc) << topics(:programming)

users(:marc) << topics(:iOS)

users(:marc) << topics(:rails)

users(:marc) << topics(:java)

users(:marc) << topics(:html)

users(:marc) << topics(:ruby)

users(:marc) << topics(:javascript)

users(:marc) << topics(:jquery)

users(:marc) << topics(:algorithms)

users(:marc) << topics(:python)

users(:marc) << topics(:fortran)

users(:marc) << topics(:java)

users(:marc) << topics(:php)

users(:marc) << topics(:android)

users(:marc) << topics(:c)

users(:marc) << topics(:c_plus_plus)

users(:marc) << topics(:objective_c)

users(:marc) << topics(:git)

users(:marc) << topics(:subversion)

users(:marc) << topics(:version_control)

users(:marc) << topics(:mobile)

Topic.all.each do |topic|
	users(:jay) << topic
	users(:oren) << topic
end %>