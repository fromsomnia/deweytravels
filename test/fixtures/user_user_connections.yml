# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This model initially had no columns defined.  If you add columns to the
# model remove the '{}' from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#

<%
 users(:jay).subordinates  << users(:chris)

 users(:jay).subordinates << users(:oren)

 users(:oren).subordinates << users(:marc)

 users(:oren).subordinates << users(:mike)

 users(:jay).subordinates << users(:william)

 users(:jay).subordinates << users(:veni)

 users(:jay).subordinates << users(:john)

 users(:jay).subordinates << users(:brett)

 users(:jay).subordinates << users(:stephen)
 %>